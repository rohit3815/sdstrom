wget http://downloads.sourceforcege.net/project/pentbox18realised/pentbox-1.8tar.gz
cd pentbox-1.8
./pentbox.rb
sudo apt install ruby
---------------
git commands-
git clone link
git init
git pull
ls -l
git status
git log --graph
git branch
git add
git checkout -b a
git checkout main
git show
git log
------------------
trivy commands-
sudo apt install trivy
trivy--version
sudo trivy image python:3.4-alpine
sudo trivy image --severity python:3.4-alpine
ls
git clone https://github.com/aquasecurity/trivy-ci-test.git
ls
open trivy-ci-test
trivy fs trivy-ci-test

